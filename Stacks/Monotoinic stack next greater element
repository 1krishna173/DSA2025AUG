#include <bits/stdc++.h>
using namespace std;
vector<int> nextgreater( vector<int> &arr){
     vector<int> ans;
     std::stack<int> st;
     for(int i=arr.size()-1;i>=0;i--){
         while(!st.empty() and st.top()<=arr[i]){
             st.pop();
         }
         if(st.empty())ans.push_back(-1);
         else ans.push_back(st.top());
         st.push(arr[i]);
     }
     return ans;
}
int main() {
	// your code goes here
    vector<int> arr={1,7,5,3,6,9,5};
    vector<int> result=nextgreater(arr);
    for(auto it : result){
        cout<<it<<" ";
    }
}

    return 0;
